import React from 'react';
import './About.css';

const About = (props) => {
    console.log(props.review);
    return (
        <div>
            <h4>This is about page</h4>
        </div>
    );
};

export default About;